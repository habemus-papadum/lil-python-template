"""Example tests for {{ package_name }}."""

import {{ package_slug }}


def test_version():
    """Test that the package has a version."""
    assert hasattr({{ package_slug }}, "__version__")
    assert isinstance({{ package_slug }}.__version__, str)
    assert len({{ package_slug }}.__version__) > 0


def test_import():
    """Test that the package can be imported."""
    assert {{ package_slug }} is not None

{% if include_ts_widgets %}

def test_widget_payload():
    """Widget helper produces a JSON-serializable payload."""
    widget = {{ package_slug }}.Widget(title="Hello", body="World")
    payload = widget.to_payload()
    assert payload["title"] == "Hello"
    assert payload["body"] == "World"
{% endif %}
